تمام 👌 خليني أقولك عمليًا إزاي تشغلي الـ APIs دي عندك:

---

## 📌 ١) تحضري الملفات

اعملي فولدر جديد جنب مشروعك (مثلاً سميه `external-apis`).
جواه اعملي ملفين:

* `graduateApi.js`
* `staffApi.js`

وحطي فيهم الأكواد اللي كتبتلك عليها قبل شوية.

---

## 📌 ٢) تعملي `package.json`

جوا فولدر `external-apis` شغلي:

```bash
npm init -y
```

وبعدين نزلي الـ express:

```bash
npm install express
```

---

## 📌 ٣) تشغلي الـ Graduate API

شغلي:

```bash
node graduateApi.js
```

هتلاقيه بيقولك:

```
Graduate API running on port 5001
```

---

## 📌 ٤) تشغلي الـ Staff API

في terminal تاني (أو نافذة جديدة)، شغلي:

```bash
node staffApi.js
```

هتلاقيه بيقولك:

```
Staff API running on port 5002
```

---

## 📌 ٥) تعدلي ملف `.env` في مشروعك الأساسي

```env
GRADUATE_API_URL=http://localhost:5001/api/graduate
STAFF_API_URL=http://localhost:5002/api/staff
```

---

## 📌 ٦) تختبري

جربي من المتصفح أو Postman:

* Graduate API:

```
http://localhost:5001/api/graduate?nationalId=12345678901234
```

هتلاقيه بيرجع:

```json
{
  "nationalId": "12345678901234",
  "college": "Engineering",
  "graduationYear": 2022
}
```

* Staff API:

```
http://localhost:5002/api/staff?nationalId=98765432109876
```

هتلاقيه بيرجع:

```json
{
  "nationalId": "98765432109876",
  "department": "Computer Science"
}
```

---

## 📌 ٧) تكملي شغل على السيستم

دلوقتي لما تعملي `register` من مشروعك الأساسي، الكود هيكلم الـ APIs دي ويجيب البيانات.

---

تحبي أخليهم يتشغلوا **مع بعض في ملف واحد** (يعني API واحدة على نفس الـ port، وجواها `/api/graduate` و`/api/staff` مع بعض) عشان ما تحتاجيش ترني اتنين سيرفرات؟
